<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 实心填充 -->
    <solid android:color="@color/nursingClass_special" />
    <!-- 描边 -->
    <stroke
        android:width="@dimen/line_size_dp_1"
        android:color="@color/bg_gainsboro" />
    <!-- 圆角 -->
    <corners android:radius="@dimen/dp_10" />

    <padding
        android:bottom="@dimen/line_size_dp_1"
        android:left="@dimen/dp_5"
        android:right="@dimen/dp_5"
        android:top="@dimen/line_size_dp_1" />

</shape>